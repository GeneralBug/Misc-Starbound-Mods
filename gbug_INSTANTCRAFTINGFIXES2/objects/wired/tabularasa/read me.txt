STabula Rasa constructs it's config with lua at runtime, so I added the disableTimer thing to a copy of the script and redirected the object to use it, but if the mod ever gets rewritten that will break